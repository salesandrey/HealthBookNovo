import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:HealthBook/app/modules/sharing/sharing_controller.dart';
import 'package:HealthBook/app/modules/sharing/sharing_module.dart';

void main() {
  initModule(SharingModule());
  // SharingController sharing;
  //
  setUp(() {
    //     sharing = SharingModule.to.get<SharingController>();
  });

  group('SharingController Test', () {
    //   test("First Test", () {
    //     expect(sharing, isInstanceOf<SharingController>());
    //   });

    //   test("Set Value", () {
    //     expect(sharing.value, equals(0));
    //     sharing.increment();
    //     expect(sharing.value, equals(1));
    //   });
  });
}
