import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:HealthBook/app/modules/sharing/sharing_page.dart';

void main() {
  testWidgets('SharingPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SharingPage(title: 'Sharing')));
    //  final titleFinder = find.text('Sharing');
    //  expect(titleFinder, findsOneWidget);
  });
}
